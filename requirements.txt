# Core dependencies for MiraTTS
# Note: ncodec is included in the MiraTTS package installation
# Note: torch/torchaudio/torchvision versions managed by lmdeploy
lmdeploy>=0.2.0

# FastAPI service dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Audio processing
scipy>=1.10.0
librosa>=0.10.0
soundfile>=0.12.0

# System monitoring
psutil>=5.9.0

# Optional but recommended for production
gunicorn>=21.0.0
